{% extends 'base.html.twig' %}

{% block title %}CommandeArticle{% endblock %}

{% block body %}
    <h1>CommandeArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_article.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commande_article.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_article_index') }}">back to list</a>

    <a href="{{ path('commande_article_edit', {'id': commande_article.id}) }}">edit</a>

    {{ include('commande_article/_delete_form.html.twig') }}
{% endblock %}
