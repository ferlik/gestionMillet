{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}

<div class="row justify-content-start">
    <div class="col-6">
        <h2>SAS PrestigePatiss</h2>
        <h3>25 Rue Emile Zola</h3>
        <h3>69120 Vaulx-en-Velin</h3>
    </div>


    <div class="col-6">
        <h3>Facture n°:{{commande.id}}</h3>
        <h3>Date: {{commande.date|date("m/d/y")}}</h3>
        <h3>client: {{commande.client.nom}}</h3>
    </div>
</div>


<table class="table table-striped">
    <thead>
        <tr>
            <th>Référence article </th>
            <th>Désignation</th>
            <th>quantité</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ commande.id }}</td>

            {% for item in commande.commandeArticles %}
            <td>{{item.article.nom}}</td>

            <td>{{item.quantite}}</td>
            {% endfor %}

        </tr>
    </tbody>
</table>




<a href="{{ path('commande_index') }}">back to list</a>

<a href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a>

{{ include('commande/_delete_form.html.twig') }}
{% endblock %}